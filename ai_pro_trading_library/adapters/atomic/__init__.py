"""Compatibility boundary for `atomic_strategy_lib` sources.

Atomic modules are inventory sources only until parity and safety checks are
complete. Canonical implementations live under `library`.
"""


# standard_bot MVP requirements — bounded ranges aligned with OpenClaw deployment
#
# Lower bound = OpenClaw current version, upper bound prevents breaking major
# upgrades. Installing this file on top of an OpenClaw baseline triggers ZERO
# package upgrades (cyqnt-trd is the only thing added).

# Core
pandas>=2.0.0,<3.0
numpy>=1.24.0,<2.0
polars>=1.0.0,<2.0
numba>=0.60.0,<0.70
pyarrow>=14.0.0,<25.0
matplotlib>=3.7.0,<4.0
scipy>=1.10.0,<2.0

# Networking
requests>=2.32.0,<3.0
websockets>=15.0.1,<16.0

# Binance SDK
binance-sdk-spot>=8.2.1,<10.0
binance-sdk-derivatives-trading-usds-futures>=10.0.1,<11.0
binance-sdk-algo>=2.6.0,<3.0
binance-common>=3.8.0,<4.0

# Test
pytest>=8.0.0

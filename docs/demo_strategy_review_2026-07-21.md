# demo_strategy Code Review — 2026-07-21

**範圍**:`binance-agentic-finance/crypto_trading` main 分支新併入的 `demo_strategy/`(`btc_multi_factor_trend` + `square_buzz_screener` 兩支參考策略,7 模組骨架)

**方法**:4 個角度(correctness / security / integration / production-readiness)× 2 支策略 = 8 個 finder agent 平行找問題,共 74 個原始發現;每個發現再由 3 個獨立 agent 對抗式驗證(嘗試推翻),67 個存活、7 個被推翻。下方為存活發現去重後(依 file:line 合併同一個 bug 在不同角度被重複抓到的情況)的 47 個獨立問題,依嚴重度分組。

**結論**:兩支策略目前都無法端到端跑完一次 pipeline(dry-run 或 live 都一樣),不能作為「照抄可用」的範本直接推廣。

---

## P0 — 兩支策略在 m04_scoring.py 都會 100% 必定崩潰(拿不到任何 verdict)

1. **`demo_strategy/btc_multi_factor_trend/strategy/m04_scoring.py:31`**
   hierarchical_combine() is called with a flat list of tier score ints instead of the [(tier_name, signals_dict, tier_weight), ...] shape the real atomic_strategy_lib.scoring.combinators.hierarchical_combine (which resolves to cyqnt_trd/blocks/verdicts.py) expects.
   > score_one() calls `hierarchical_combine([t['score'] for t in tiers])` e.g. `hierarchical_combine([3,2,2,2,1,1])`. The real function does `for tier_name, signals, tier_weight in tiers:` and tries to unpack each int, raising `TypeError: cannot unpack non-iterable int object`. Verified by direct execution. This fires for EVERY symbol on every run (including the plain `python3 run.py` default invocation), so the pipeline crashes before a single verdict/decision is ever produced — the scoring module as shipped cannot run at all.

2. **`demo_strategy/square_buzz_screener/strategy/m04_scoring.py:28`**
   score_one() calls the shared hierarchical_combine atom with the wrong argument shape, guaranteeing a TypeError on every run instead of reusing it correctly.
   > `total = hierarchical_combine([t["score"] for t in tiers])` passes a flat list of ints (e.g. [3,-1,2,1,2,2,1,2,2]). The atom this resolves to via `atomic_strategy_lib.scoring.combinators` is `cyqnt_trd/blocks/verdicts.py:219 hierarchical_combine(tiers_or_signals, weights=None)`, which requires either a list of AtomicSignal objects (has `.direction`) or `(tier_name, signals_dict, tier_weight)` tuples, and returns an `AtomicScore` dataclass — not a number. I confirmed by executing `hierarchical_combine([3,-1,2,1,2,2,1,2,2])` in the repo's `.venv-mvp` that it raises `TypeError: cannot unpack non-iterable int object` immediately. This means `run.py` crashes on the very first candidate; the 'reference implementation' does not actually run end-to-end.

3. **`demo_strategy/btc_multi_factor_trend/strategy/m04_scoring.py:32`**
   verdict_with_gate(total, thresholds) passes the plain config thresholds dict as the `gates` argument, but the real function expects a list of per-gate result dicts (each with a 'passed' key); it also never applies the configured STRONG_CANDIDATE/CANDIDATE/WATCHLIST thresholds.
   > If line 31's crash were fixed, `verdict_with_gate(11, {'STRONG_CANDIDATE':10,'CANDIDATE':6,'WATCHLIST':2})` would execute `failed_gates = [g for g in gates if not g.get('passed', True)]`, iterating the dict's string keys (e.g. 'STRONG_CANDIDATE') and calling `.get()` on a str, raising `AttributeError: 'str' object has no attribute 'get'` (verified by direct execution). Additionally, even if that crash didn't happen, the library's own defaults (strong_min=8.0/candidate_min=5.0/watchlist_min=2.0) would silently be used instead of the config's 10/6/2, since the thresholds are never passed via the `strong_min=`/`candidate_min=`/`watchlist_min=` keywords.


## P0 — square_buzz_screener 的候選發現機制本身是空殼,永遠找不到任何標的

4. **`demo_strategy/square_buzz_screener/strategy/m01_universe.py:19`**
   discover_universe() is built entirely on `atomic_strategy_lib.data.search.hotrank_fetch` / `trending_fetch`, which the shim's own module docstring marks "NOT PORTED — stubs" and which unconditionally return `[]` regardless of arguments.
   > Every run of the reference "Binance Square attention screener" silently discovers zero candidates and prints a normal-looking "0 candidates" report with exit code 0 — nothing warns the operator that the core data source is fake, so a team member copying this template for a live strategy would have no signal that universe discovery does not work until they notice trades never happen.


## P0 — 方向判斷 + 倉位計算全部接錯,decision 產出的是錯誤型別

5. **`demo_strategy/btc_multi_factor_trend/strategy/m05_decision.py:29`**
   direction_from_multi_factor(votes) is called with a positional list of 'LONG'/'SHORT' strings, but the real function expects five keyword categorical args (trend_signal=, momentum_signal=, ... each 'BULLISH'/'BEARISH'/'NEUTRAL'/None) and returns a dict, not a direction string.
   > `votes` (e.g. `['LONG','LONG']`) binds positionally to `trend_signal`, which never equals 'BULLISH'/'BEARISH', so it contributes nothing to bullish/bearish weight; the function always returns a non-empty dict like `{'direction':'WATCH', 'agreement_pct':50.0, ...}`. Because a non-empty dict is truthy, `direction = direction_from_multi_factor(votes) or 'WATCH'` assigns the WHOLE DICT to `direction`, never a string. `decision['direction']` therefore is always a dict object. Downstream, m06_execution.py's `if row['decision']['direction'] == 'WATCH'` never matches (so watch-only rows are never skipped) and `side='SELL' if decision['direction']=='LONG' else 'BUY'` always evaluates to 'BUY' regardless of the real position side — meaning a protective stop for what should be a LONG position would place a BUY stop instead of a SELL stop.

6. **`demo_strategy/square_buzz_screener/strategy/m05_decision.py:33`**
   decide_one() calls direction_from_multi_factor() with a positional list of pre-computed LONG/SHORT strings instead of the atom's real keyword/BULLISH-BEARISH-NEUTRAL contract, so decision['direction'] is always the raw result dict, never a plain direction string.
   > `direction = direction_from_multi_factor(votes) or "WATCH"` where `votes` is a list like `["LONG","SHORT"]`. The real atom, `cyqnt_trd/blocks/decision.py:130 direction_from_multi_factor(trend_signal=None, momentum_signal=None, ...)`, expects keyword-only BULLISH/BEARISH/NEUTRAL strings per named dimension and returns a dict like `{"direction": "WATCH", ...}`. Passed positionally, `votes` becomes `trend_signal`; since a list never equals "BULLISH"/"BEARISH" it falls to NEUTRAL, and the function returns a non-empty (truthy) dict every time. I confirmed via direct execution that `direction_from_multi_factor(['LONG','SHORT','LONG']) or 'WATCH'` evaluates to the whole dict, not a string. Downstream in m06_execution.py this breaks `decision['direction'] == 'WATCH'` (always False, so WATCH candidates are never filtered from execution), `"SELL" if decision['direction']=='LONG' else "BUY"` (always mis-resolves), and `market_order(symbol, side=decision['direction'], ...)` would pass an entire dict as the order side to a live order call.

7. **`demo_strategy/btc_multi_factor_trend/strategy/m05_decision.py:45`**
   Position sizing is computed with a unit mismatch (percent vs. fraction) and a dict-vs-scalar mismatch that crashes `decide_one()` on every CANDIDATE/STRONG_CANDIDATE verdict — i.e. exactly the rows that would go on to size and place a real order — and there is no independent sanity cap anywhere downstream to bound `size_usdt`/`leverage` if this is naively patched.
   > `stop_pct`/`risk_pct` are pre-divided by 100 into fractions (e.g. 0.05, 0.03) at lines 41-42, then passed into `atomic_strategy_lib.decision.sizing.fixed_risk_pct(balance, risk_pct, stop_pct)` (cyqnt_trd/blocks/sizing.py:222) which itself divides by 100 again internally and returns a **dict** (`{"notional":..,"leverage":..,...}`), not a float; line 46's `size_usdt / balance` then raises `TypeError: unsupported operand type(s) for /: 'dict' and 'int'` on any non-SKIP/WATCHLIST verdict, verified interactively by re-executing the sizing function with the same arguments. Because this file is the shipped reference template other teams are told to copy, the likely 'fix' — pulling `size_usdt["notional"]` out of the dict without correcting the pre-divided percent/fraction units — would silently mis-scale real risk/leverage by ~100x with no other check in `m06_execution.py` or `execute_all` that caps notional size or leverage before dispatching a real order for real account balance.

8. **`demo_strategy/square_buzz_screener/strategy/m05_decision.py:48`**
   decide_one() treats fixed_risk_pct()'s return value as a plain USDT float and calls round() on it, but the atom returns a dict, which crashes and also double-applies the percent-to-fraction conversion.
   > `size_usdt = fixed_risk_pct(balance, risk_pct, stop_pct)` then `round(size_usdt, 2)` at line 52. `cyqnt_trd/blocks/sizing.py:223 fixed_risk_pct(balance, risk_pct=2.0, stop_pct=5.0, ...)` returns `{"notional":..., "leverage":..., "risk_amount":..., "risk_pct_actual":...}`, not a scalar. I confirmed executing it directly raises `TypeError: type dict doesn't define __round__ method`. Separately, m05 already divides `risk_pct`/`stop_pct` by 100 before calling (passing 0.02/0.05), while the atom's own docstring says to pass e.g. 2.0 for 2% and internally divides by 100 again — a double-conversion that (were the dict/round crash not present) would under-size every position by 100x. This path is currently unreachable only because the m04 crash above happens first; fixing that crash would immediately expose this one.

9. **`demo_strategy/btc_multi_factor_trend/strategy/m05_decision.py:48`**
   stop_price is only computed when signals['_entry_price'] is truthy, but m03_signals.py's compute_signals() never sets an '_entry_price' key, so stop_price is always None; the call also passes a keyword `entry=` that doesn't exist on compute_stop_price (real param name is entry_price).
   > `signals.get('_entry_price')` is always None (no module ever writes that key), so every decision's `stop_price` is None regardless of mode/verdict — contradicting the README's example output (`"stop_price": 67672.7`). If a future edit ever populated `_entry_price`, the call `compute_stop_price(entry=..., direction=..., stop_pct=...)` would immediately raise `TypeError: compute_stop_price() got an unexpected keyword argument 'entry'` since the real signature is `compute_stop_price(entry_price, direction, stop_pct=None, atr_val=None, multiplier=1.0)`.

10. **`demo_strategy/square_buzz_screener/strategy/m05_decision.py:30`**
   `if funding_signal in ("LONG", "SHORT"): votes.append(funding_signal)` compares against literals that `signals["derivatives"]["funding_signal"]` (sourced from `signal_direction`) can never contain — that field only ever holds `"BULLISH"/"BEARISH"/"NEUTRAL"`.
   > Regardless of the actual funding-rate state, this branch is permanently dead code (the string sets never intersect), so the funding/derivatives factor never contributes a direction vote in `decide_one()`, silently defeating one of the four intended voting inputs.


## P1 — 訊號層讀錯 key,分數永遠算錯但不會崩潰(最難發現的一類)

11. **`demo_strategy/btc_multi_factor_trend/strategy/m03_signals.py:105`**
   _macd_signal() reads keys 'macd_current'/'signal_current'/'histogram_current'/'histogram_increasing' from macd_compute()'s result, but the real dict uses 'current_macd'/'current_signal'/'current_histogram' (reversed word order) and has no 'histogram_increasing' key at all.
   > For any real MACD computation, `m.get('macd_current')`, `m.get('signal_current')`, `m.get('histogram_current')` all miss and return None, and `m.get('histogram_increasing', False)` always returns False. `signals['macd']` is therefore always `{'macd': None, 'signal': None, 'histogram': None, 'hist_increasing': False}` no matter the real MACD state. In m04_scoring.py's `_score_macd()`, `hist = sig.get('histogram') or 0.0` becomes 0.0, so neither `hist>0` nor `hist<0` fires and the function always falls through to `return _tier('macd_momentum', thr.get('negative', -1), ...)` — Tier 3 (macd_momentum, README says up to +2/-2) silently scores exactly -1 for every symbol on every run. It also means the MACD histogram vote in m05_decision.py's `_collect_direction_votes()` never fires.

12. **`demo_strategy/square_buzz_screener/strategy/m03_signals.py:106`**
   MACD signal reads `m.get("histogram_current")` and `m.get("histogram_increasing", False)`, but `macd_compute()` (cyqnt_trd/compat/atomic_signals/computes.py) actually returns `current_histogram` (word order reversed) and has no `histogram_increasing` key at all.
   > `signals["macd"]["histogram"]` is always `None` and `hist_increasing` is always `False` for every candidate/every run, so `_score_macd()` in m04_scoring.py always falls through to the `"MACD -ve"` branch (score -1), and in m05_decision.py `hist = signals["macd"].get("histogram") or 0` is always `0`, so MACD never casts a LONG/SHORT vote regardless of the real MACD state.

13. **`demo_strategy/btc_multi_factor_trend/strategy/m03_signals.py:118`**
   'funding_zone' is read from funding_extreme_detect()'s 'direction' key (which only ever holds 'BULLISH'/'BEARISH'/'NEUTRAL'), but the real zone labels ('SQUEEZE'/'MILD_SQUEEZE'/'CROWDED'/'NEUTRAL') that _score_derivatives() checks for live under the 'zone' key.
   > Even with an extreme funding rate (e.g. rate=-0.002, which funding_extreme_detect classifies as zone='SQUEEZE'), `info.get('direction', 'NEUTRAL')` returns 'BULLISH', not 'SQUEEZE'. m04_scoring.py's `_score_derivatives()` checks `z in ('SQUEEZE','EXTREME_NEGATIVE')` / `('BUILDING_LONG','BUILDING_SHORT')` / `('CROWDED_LONG','CROWDED_SHORT')` — none of which 'funding_zone' can ever equal — so Tier 4 (derivatives, README says up to +2/-2) always falls through to the 'neutral' (score 0) branch regardless of how squeezed or crowded funding actually is.

14. **`demo_strategy/square_buzz_screener/strategy/m03_signals.py:117`**
   `_derivatives_signal` sets `"funding_zone": info.get("direction", "NEUTRAL")`, but `direction` on `funding_extreme_detect()`'s return dict is the bullish/bearish bias ("BULLISH"/"BEARISH"/"NEUTRAL"), not the actual zone label; the zone label ("SQUEEZE"/"MILD_SQUEEZE"/"CROWDED"/"NEUTRAL") lives under the `zone` key, which is never read.
   > Even during an extreme negative funding squeeze, `funding_extreme_detect` returns `{"zone": "SQUEEZE", "direction": "BULLISH", ...}`; `signals["derivatives"]["funding_zone"]` ends up as `"BULLISH"`, which never matches any of the literal strings `_score_derivatives()` checks for in m04_scoring.py (`"SQUEEZE"`, `"CROWDED_LONG"`, etc.), so the T6 derivatives tier always falls through to the `"neutral"` (0) branch regardless of real funding stress.

15. **`demo_strategy/btc_multi_factor_trend/strategy/m03_signals.py:141`**
   _resonance_signal() reads keys `aligned`/`dominant`/`score` from multi_tf_resonance(), but the actual (stub) implementation in atomic_compat/atomic_strategy_lib/signals/trend.py returns `{resonance, agreement_pct, tf_breakdown}` — the keys never match, so the T6 multi-timeframe-resonance factor silently defaults to aligned=False/dominant=UNKNOWN/score=0 on every run.
   > Because m04_scoring.py's _score_resonance() treats `dominant not in (BULLISH, BEARISH)` and `aligned=False` as 'conflicting', the resonance tier (documented as worth up to +2/-1 in the README) silently and permanently scores -1 for every symbol on every run, with no exception raised — the strategy is quietly running with only 5 of its advertised 6 factors, and nobody would notice from a passing test run.

16. **`demo_strategy/btc_multi_factor_trend/strategy/m03_signals.py:142`**
   multi_tf_resonance(directions), as imported through the atomic_compat shim (atomic_compat/atomic_strategy_lib/signals/trend.py), resolves to a hard-coded stub that ignores its input and always returns {'resonance': False, 'agreement_pct': 0.0, 'tf_breakdown': {}} instead of the real working implementation of the same name in cyqnt_trd/compat/atomic_signals/computes.py.
   > Regardless of real 1h/4h/1d trend agreement, `reso` never contains 'aligned'/'dominant'/'score' keys, so `_resonance_signal()` always returns `{'aligned': False, 'dominant': 'UNKNOWN', 'score': 0}`. In m04_scoring.py's `_score_resonance()`, `sig.get('aligned')` is always False and `sig.get('dominant') in ('BULLISH','BEARISH')` is always False (it's 'UNKNOWN'), so Tier 6 (resonance, README says up to +2/-1) always scores exactly -1 ('TFs conflicting') even on perfectly aligned multi-timeframe uptrends.

17. **`demo_strategy/square_buzz_screener/strategy/m03_signals.py:137`**
   _resonance_signal() trusts multi_tf_resonance(), which resolves to a hardcoded stub that always returns 'not aligned / UNKNOWN' regardless of the actual per-timeframe trend directions passed in.
   > `r = multi_tf_resonance(dirs)` imports from `atomic_strategy_lib.signals.trend`, whose `multi_tf_resonance` (atomic_compat/atomic_strategy_lib/signals/trend.py:12) is a stub: `return {"resonance": False, "agreement_pct": 0.0, "tf_breakdown": {}}` for ANY input — it never calls the real, working implementation that already exists at `cyqnt_trd/compat/atomic_signals/computes.py:465 multi_tf_resonance(tf_directions)` (which does real aligned/dominant/score logic). I confirmed by executing `multi_tf_resonance({'4h':'BULLISH','1h':'BULLISH','1d':'BULLISH'})` that it still returns the dummy dict. Because the stub's keys (`resonance`, `agreement_pct`, `tf_breakdown`) don't match what m03 reads (`aligned`, `dominant`), `_resonance_signal` always reports `aligned=False, dominant='UNKNOWN'`, and m04_scoring.py's `_score_resonance` (m04_scoring.py:136) always falls through to '-1 TFs conflicting' for every candidate, no matter how well the timeframes actually agree — a systematic, silent scoring defect that no test in the demo catches.

18. **`demo_strategy/btc_multi_factor_trend/strategy/m03_signals.py:120`**
   oi_value always reports 0 because open_interest_fetch() returns an OpenInterest dataclass, never a dict, so the `isinstance(oi, dict)` guard always takes the else branch.
   > Even when open interest is fetched successfully and has a large, meaningful oi_value, `oi.get('oi_value', 0) if isinstance(oi, dict) else 0` evaluates the isinstance check against an `OpenInterest` dataclass instance (False), so `signals['derivatives']['oi_value']` is always 0 in the output/report regardless of real market data (lower severity — this field is not consumed by scoring/decision, only shown in the raw signals JSON, but it silently reports fabricated data).

19. **`demo_strategy/square_buzz_screener/strategy/m03_signals.py:149`**
   `_volume_signal` reads `surge.get("surge", False)`, but `volume_surge_detect()` (cyqnt_trd/compat/atomic_signals/volume_detectors.py) returns the boolean under the key `is_surge`, not `surge`.
   > `signals["volume"]["surge"]` is always `False` even during a genuine 3x/5x volume surge, so `_score_volume()` in m04_scoring.py can never award the advertised `"surge"` score (2 points, per config.json `scoring.tiers.volume.surge`); T9 is permanently capped at `"elevated"`(1) or `"flat"`(0).

20. **`demo_strategy/square_buzz_screener/strategy/m04_scoring.py:96`**
   `_score_rsi` checks `z in ("BULLISH_NEUTRAL", "BEARISH_NEUTRAL")` to award the top "favorable" (+2) score, but `rsi_zone_detect()` (cyqnt_trd/compat/atomic_signals/computes.py) only ever returns `"OVERBOUGHT"`, `"OVERSOLD"`, or `"NEUTRAL"` — those two literal strings are never produced.
   > For any RSI value, this branch is unreachable dead code; the RSI tier (T4) can never award its documented "favorable" score of 2 (per config.json `scoring.tiers.rsi_zone.favorable`) — the maximum achievable RSI score is capped at 1 ("neutral"), silently under-scoring every candidate that should be in a favorable RSI zone.

21. **`demo_strategy/square_buzz_screener/strategy/m04_scoring.py:120`**
   `_score_derivatives` checks `z in ("BUILDING_LONG", "BUILDING_SHORT")` and `z in ("CROWDED_LONG", "CROWDED_SHORT")`, but no function in the signal chain ever produces those exact strings: `funding_extreme_detect()` only emits `"CROWDED"` (no LONG/SHORT suffix), and OI-building state (`oi_anomaly_detect`, which returns `"BUILDING"`/`"UNWINDING"`) is never wired into `signals["derivatives"]` at all.
   > Independent of the m03 key-naming bug (line 117), these two branches are unreachable dead code by construction — a crowded-long or crowded-short funding regime, or an OI-building regime, can never be scored/penalized as intended; T6 always resolves to `"neutral"` (0).


## P0 — 下單/執行層打不通,而且沒有真正的安全網

22. **`demo_strategy/btc_multi_factor_trend/strategy/m06_execution.py:74`**
   The sole anti-slippage/stale-price guard on the live order path is non-functional: `price_deviation_check(symbol, entry, max_dev, profile)` is called with 4 positional args (symbol string, one price, max_dev, profile string) but `atomic_strategy_lib.risk.limits.price_deviation_check` (cyqnt_trd/blocks/limits.py:124) has the signature `price_deviation_check(estimated_price, live_price, max_deviation_pct=2.0)` — it takes no `symbol`/`profile`, never fetches a live price itself, and merely diffs two caller-supplied numbers.
   > Run `python3 run.py --execute --live --mode aggressive` on a STRONG_CANDIDATE row: `price_deviation_check("BTCUSDT", 71234.5, 2.0, "default")` immediately raises `TypeError: price_deviation_check() takes from 2 to 3 positional arguments but 4 were given` (or, once someone 'fixes' the call arity/types without noticing the semantic gap, the function would just compare `entry` to itself since no fresh price is ever fetched, so `deviation_pct` is always 0 and the guard always passes) — either the whole live path crashes, or the price-moved-since-signal check silently provides zero real protection, letting a stale/slipped price go straight to `market_order`/`stop_order` with a real account.

23. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:65`**
   `ok, cur_price = price_deviation_check(symbol, entry, max_dev, profile)` passes 4 positional args and unpacks a 2-tuple, but the real cyqnt_trd.blocks.limits.price_deviation_check(estimated_price, live_price, max_deviation_pct=2.0) takes 3 numeric args (no `profile`) and returns a dict with keys can_enter/deviation_pct/reason.
   > Calling with `symbol` (a string) as `estimated_price` and 4 positional args to a 3-parameter function raises `TypeError: price_deviation_check() takes from 2 to 3 positional arguments but 4 were given` — this fires for every candidate that reaches execution (dry-run or live), so the entire order-placement path has never actually run end-to-end; it is also never actually fetching a live price to compare against, defeating the purpose of the guard even once fixed.

24. **`demo_strategy/btc_multi_factor_trend/strategy/m06_execution.py:81`**
   quantize()/round_to_tick() are called with the whole ExchangeFilter dataclass returned by exchange_filter_fetch() instead of its .step_size/.tick_size float fields.
   > `exchange_filter_fetch()` returns an `ExchangeFilter` dataclass (never a dict, so the `or {}` fallback never triggers). `quantize(decision['size_usdt']/entry, filt)` executes `if step <= 0:` where `step` is the ExchangeFilter instance, raising `TypeError: '<=' not supported between instances of 'ExchangeFilter' and 'int'` (verified: plain dataclasses have no __le__). This line runs BEFORE the `if not live:` dry-run branch, so simply running `python3 run.py --execute` (no --live needed) on any symbol that passes the verdict gate and isn't WATCH crashes _place_one() before it can even print the dry-run order plan.

25. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:70`**
   `filt = exchange_filter_fetch(...)` returns an ExchangeFilter dataclass, but it is passed whole into `quantize(value, filt)` and `round_to_tick(price, filt)`, both of which expect a bare float (step_size / tick_size), not the filter object.
   > Once the price_deviation_check crash above is fixed, this line raises `TypeError: '<=' not supported between instances of 'ExchangeFilter' and 'int'` inside quantize()/round_to_tick() — order quantity and stop price can never actually be computed, so no order (dry-run or live) would ever be produced by this reference execution module as written.

26. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:71`**
   `quantize(qty_estimate, filt)` and `round_to_tick(price, filt)` pass the whole `filt` object (an `ExchangeFilter` dataclass, or `{}` on failure) as the numeric `step`/`tick_size` argument, but both functions (cyqnt_trd/exec_cli/filters.py) expect a plain float step/tick size and immediately do `if step <= 0` / floor-division against it.
   > `exchange_filter_fetch` succeeds and returns an `ExchangeFilter` instance (or the `or {}` fallback returns `{}`); either way, `quantize(value, filt)` executing `if step <= 0` compares a dataclass/dict to `0`, raising `TypeError: '<=' not supported between instances of 'ExchangeFilter' and 'int'` (or dict vs int), crashing before an entry/stop price can be computed.

27. **`demo_strategy/btc_multi_factor_trend/strategy/m06_execution.py:95`**
   None of the three exchange-mutating calls in `_place_one` (`set_leverage` line 96, `market_order` lines 99-100, `stop_order` lines 103-105) forward the `live` flag that gated entry into this branch, and `market_order`/`stop_order` are additionally called with the wrong keyword (`qty=` instead of the shim's `quantity=`), so the code that is supposed to execute real orders when `--execute --live` is passed cannot actually do so, yet still reports success.
   > With `execution.enabled=execution.live=True` and a STRONG_CANDIDATE row, `market_order(symbol, side=..., qty=qty, market=mode, profile=profile)` raises `TypeError: market_order() got an unexpected keyword argument 'qty'` today (verified against the shim signature `market_order(symbol, side, quantity, ..., live=False)` in atomic_compat/atomic_strategy_lib/execution/orders.py:21-22). If a developer later does the minimal fix of renaming `qty`→`quantity` without also adding `live=live`, every call (`set_leverage`, `market_order`, `stop_order`) falls back to each function's own safe default (`live=False`→`dry_run=True`, and cyqnt_trd/exec_cli's own documented 'all functions default to dry_run=True' contract), so real orders are silently never sent — but `_place_one` still returns `{"status": "filled", ...}` and calls `position_create(...)`, so the JSON report and any downstream automation believe a real position and protective stop exist on the exchange when neither was placed, which is dangerous if the operator relies on that reported 'filled' stop-loss for risk management on an actual open position.

28. **`demo_strategy/btc_multi_factor_trend/strategy/m06_execution.py:99`**
   market_order() is called with keyword qty=, but the atomic_strategy_lib shim's market_order() only accepts quantity=; separately, `live` is never forwarded to market_order/stop_order/set_leverage, so even if the keyword bug were fixed they would silently default to dry_run=True regardless of --live.
   > An operator runs `python3 run.py --execute --live --mode balanced` expecting real orders. Once the earlier quantize/price_deviation_check bugs are patched, this line still raises `TypeError: market_order() got an unexpected keyword argument 'qty'`; if a well-meaning fix renames qty->quantity without also passing live=True, set_leverage(symbol, decision['leverage'], profile=profile) and the order calls at lines 99-105 all fall back to their dry_run=True/live=False defaults, so the JSON report shows `"status": "filled"` and a fake leverage/order response while nothing was actually sent to Binance — a silent false-positive that is dangerous for real capital.

29. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:85`**
   `market_order(...)` and `stop_order(...)` are called with keyword `qty=qty`, but both shims (atomic_compat/atomic_strategy_lib/execution/orders.py) define the parameter as `quantity`, and neither call forwards `live=live` (both default to `live=False`, i.e. always dry-run internally).
   > Once execution reaches the live path (`live=True`, `mode=="futures"`), `market_order(symbol, side=..., qty=qty, market=mode, profile=profile)` raises `TypeError: market_order() got an unexpected keyword argument 'qty'`. Even if that were fixed, since `live=live` is never passed through, the underlying call would still use the default `live=False` → `dry_run=True`, so a `--execute --live` run would silently never place a real order.

30. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:91`**
   `position_create(symbol=symbol, side=decision["direction"], qty=qty, entry=entry, stop=stop_px, mode=mode)` uses keyword names (`side`, `qty`, `entry`, `stop`) that don't exist on the real function, and omits several required parameters (`direction`, `entry_price`, `stop_price`, `stop_pct`, `leverage`, `notional`, `max_loss` — per cyqnt_trd/standard_bot/orchestration/state.py's signature).
   > Reaching this line (after fixing the earlier order-placement bugs) raises `TypeError: position_create() got an unexpected keyword argument 'side'`, so a filled position is never persisted to the position-tracking store.

31. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:17`**
   Only `price_deviation_check` is wired into the live execution path; `daily_loss_check`, `circuit_breaker_check`, `max_exposure_check`, and `liquidation_check` all exist in `atomic_strategy_lib.risk.limits` but are never imported or called, so there is no daily-loss kill switch or circuit breaker protecting the real-money path.
   > A batch of hyped tokens all clear the STRONG_CANDIDATE bar in one run (e.g. during a coordinated pump on Binance Square) and the bot opens 3 concurrent live losing positions back-to-back with no cumulative-loss check anywhere in `execute_all`/`_place_one`; because `daily_loss_check`/`circuit_breaker_check` are never invoked, nothing stops the bot from continuing to place new live orders on the very next run even if the account has already blown through an acceptable daily loss threshold.

32. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:24`**
   Besides the (broken) price_deviation_check and a same-run position counter, execute_all() wires in none of cyqnt_trd/blocks/limits.py's account-level guards (daily_loss_check, max_exposure_check, circuit_breaker_check) before placing real orders — there is no check of current unrealized PnL, total notional exposure vs. balance, or liquidation proximity.
   > A losing streak across several screener runs in one day has no daily-loss circuit breaker to halt further entries; combined with the max_concurrent_positions gap above, the account can keep opening new leveraged positions after it has already blown through a reasonable daily-loss or exposure threshold, with only `--execute`/`--live` gating whether orders are real, not whether they are safe to place.

33. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:41`**
   `max_concurrent_positions` is enforced purely via a local `filled_count` variable that starts at 0 every run — it never calls `position_list`/`position_open_list` (both available via atomic_strategy_lib.orchestration.state, already imported for `position_create`) to check how many positions are already open from a prior run.
   > If the screener is run on a schedule (its natural periodic use-case, per the README's `--execute --live` example), and a previous run already opened 3 positions up to the configured cap, the next run's `filled_count` resets to 0 and can open 3 more on top — silently exceeding the intended max_concurrent_positions=3 risk limit with real capital.

34. **`demo_strategy/btc_multi_factor_trend/strategy/m05_decision.py:25`**
   Position sizing reads a static hardcoded account_balance_usdt from config.json rather than querying real account equity, even though atomic_strategy_lib.data.account.account_balance_fetch exists and is available.
   > A user copies this template, sets account_balance_usdt=1000 once, then over weeks their real balance drops to $300 after losses or grows to $5000 after wins; fixed_risk_pct(balance=1000, risk_pct, stop_pct) keeps sizing every trade off the stale $1000 figure, silently risking a much larger or smaller fraction of real equity than the configured risk_pct intends, with no reconciliation check or warning anywhere in the pipeline.

35. **`demo_strategy/square_buzz_screener/strategy/m05_decision.py:45`**
   Live order sizing is derived solely from the static `trade.account_balance_usdt` config value (default 1000 in config.json) with no call to the library's `account_balance_fetch()` to verify against the real exchange balance, and `m06_execution.py` applies no independent max-notional/exposure cap before placing real orders.
   > An operator tests the strategy against a paper/small account and leaves `trade.account_balance_usdt: 1000` in config.json (or later funds a much smaller live account without updating it), then runs `python3 run.py --execute --live --min-verdict STRONG_CANDIDATE`. `fixed_risk_pct(balance=1000, risk_pct=2.0, stop_pct=5.0)` sizes every filled trade against the stale $1000 figure — up to 3 concurrent positions (`max_concurrent_positions`) each leveraged up to 3x per config — with nothing in `m06_execution.py` re-checking that computed notional against the account's actual current equity/margin before calling `market_order`, so a stale or fat-fingered config value directly inflates real notional exposure with no safety net.

36. **`demo_strategy/square_buzz_screener/config/config.json:53`**
   `trade.account_balance_usdt` is a hardcoded constant (1000) used directly for position sizing in m05_decision.py, instead of fetching the real account balance via `atomic_strategy_lib.data.account.account_balance_fetch` (already available in the shim but unused anywhere in this strategy).
   > Any user/environment whose actual futures account balance differs from 1000 USDT (larger, smaller, or drained by other concurrently-running strategies on the same account) gets position sizes computed against a fictitious balance — e.g. an account with $200 real equity would size trades as if it had $1000, risking 5x the intended per-trade risk.

37. **`demo_strategy/square_buzz_screener/strategy/m02_data.py:32`**
   fetch_market_bundle() hardcodes `market="spot"` for all kline/signal computation regardless of `cfg.execution.mode`, which is "futures" in config.json — technical signals (EMA/RSI/MACD/ATR/volume/resonance) and the `entry` reference price are all derived from spot candles while orders are placed on the futures/perp market via m06_execution.py.
   > During a funding-driven basis dislocation (common around funding settlement or a squeeze — exactly the kind of event this screener's derivatives tier is meant to catch), spot and perp prices diverge; the price_deviation_check guard and quantity/stop math end up validated against the wrong instrument's price, potentially passing a stale-price check for the perp that would have failed if it had used real futures price data.

38. **`demo_strategy/btc_multi_factor_trend/run.py:66`**
   The --profile CLI flag only overrides cfg['_profile'] (consumed by m02_data.py for market data), never cfg['execution']['profile'], so real order placement always uses whatever 'profile' is hardcoded in config.json regardless of what the operator passed on the command line.
   > A trader runs `python3 run.py --profile subaccount_b --execute --live`, intending every API call — including order placement — to use their `subaccount_b` binance-cli credentials. m06_execution.py:35 (`profile = exc.get('profile', 'default')`) ignores that and always uses config.json's execution.profile ("default"), so real orders are placed under the wrong Binance account/API key while the operator believes they targeted subaccount_b.


## P2 — 錯誤處理 / 可觀測性缺口

39. **`demo_strategy/btc_multi_factor_trend/strategy/m02_data.py:47`**
   Funding-rate and open-interest fetches are wrapped in bare `except Exception: ... = None` with zero logging, unlike the underlying kline HTTP layer which does log retries/give-ups to stderr.
   > If Binance funding/OI endpoints start returning malformed JSON or time out (rate-limit, outage), `funding`/`oi` silently become None with no print/log/metric anywhere. m03_signals.py:113 then substitutes `fr = 0.0` (a real, meaningful 'neutral' funding value) for what was actually a failed fetch, so the derivatives tier score is computed from fabricated data and no one — not even someone tailing stdout — can tell the difference between 'funding is genuinely neutral' and 'the funding API call failed.'

40. **`demo_strategy/btc_multi_factor_trend/strategy/m02_data.py:60`**
   price = primary_candles[-1].close if primary_candles else 0.0 silently substitutes 0.0 for a total kline-fetch failure instead of raising or flagging degraded data.
   > If kline_fetch_multi_tf returns only empty candle lists for the primary timeframe (e.g. Binance outage that exhausts the built-in 3 retries), price becomes 0.0 and is written into pipeline_result.json indistinguishable from a real $0 price. If a row with price=0.0 ever reached m06_execution.py:81 (`quantize(decision['size_usdt'] / entry, filt)`), it would raise a ZeroDivisionError instead of being cleanly rejected as 'no data available'.

41. **`demo_strategy/btc_multi_factor_trend/strategy/__init__.py:26`**
   The universe loop (and run.py's __main__ guard) has no per-symbol try/except and no top-level handler besides KeyboardInterrupt, so one bad symbol aborts the entire multi-symbol run with no structured logging and no partial results saved.
   > With `--symbols BTCUSDT ETHUSDT DOGEUSDT`, if fetch_market_bundle/compute_signals/score_one/decide_one raises for DOGEUSDT (malformed candle data, a transient exception not covered by the narrow try/except in m02_data.py, or any of the API-signature bugs above), the exception propagates out of run(), out of main() (only `except KeyboardInterrupt` is caught in run.py:90-93), and the process exits with a raw traceback — discarding the already-computed BTCUSDT/ETHUSDT verdicts (out_path.write_text only happens after run() returns) and logging nothing about what verdict any symbol received before the crash.

42. **`demo_strategy/square_buzz_screener/strategy/__init__.py:24`**
   The per-candidate loop in run() (fetch_market_bundle → compute_signals → score_one → decide_one) has no try/except, unlike the individual API calls inside m02_data.py which are each wrapped.
   > Once any of the above signature-mismatch bugs (or a malformed API payload causing an AttributeError deep in a block function) triggers for one symbol, the exception propagates out of the whole `for cand in candidates` loop and kills the entire multi-symbol run — a single bad symbol makes the screener report zero results for every other candidate too, with no partial output and no indication which symbol caused it.

43. **`demo_strategy/square_buzz_screener/strategy/m02_data.py:28`**
   All four API calls (kline, ticker, funding, OI) are wrapped in bare `except Exception:` that silently sets the value to `{}`/`None` with zero logging — no message is printed or logged when Binance times out, rate-limits, or returns malformed data, and only the kline path (via the shim's internal `_http_get_json`) has retry/backoff; ticker_24h_fetch/funding_rate_current/open_interest_fetch go through cyqnt_trd.data_cli subprocess calls with no retry at all.
   > During a rate-limited scan across 10-15 candidates, funding/OI/ticker calls silently fail one time each (no retry) and degrade to None/0.0, which m03_signals.py then treats as legitimate neutral data (`funding_rate: 0.0`, `oi_value: 0`) — an operator watching the leaderboard has no way to tell "this token's derivatives tier scored neutral because data is missing" apart from "it's genuinely neutral", and could act on a screener result silently built on partial data with no logged warning.


## P2 — 架構/重用違規(跟既有 cyqnt_trd/blocks 重複或違反 README 自訂分層)

44. **`demo_strategy/btc_multi_factor_trend/strategy/m03_signals.py:70`**
   _ema_signals() computes a threshold-gated categorical `direction` (BULLISH/BEARISH/NEUTRAL from `aligned_count >= 2`) inside m03_signals.py, violating the README's own stated rule that the signals module only computes numbers with no threshold judgment, and duplicates (with different, undocumented price-vs-EMA semantics) the existing ma_alignment() atom.
   > A teammate tuning scoring thresholds (the documented 'high change frequency' module per the README's own table) would naturally look in m04_scoring.py/config.json, not realize that the EMA BULLISH/BEARISH cutoff (2-of-3 aligned) is hardcoded in m03_signals.py and not driven by cfg.scoring.tiers at all — changing config.json's ema_trend tier weights won't change which symbols get classified BULLISH vs NEUTRAL, only how many points that classification is worth. Existing atom: cyqnt_trd/blocks/indicators.py:366-382 ma_alignment() already implements an MA-stack alignment classifier but with different (MA-vs-MA, not price-vs-MA) semantics — now there are two divergent 'alignment' classifiers in the codebase.

45. **`demo_strategy/square_buzz_screener/strategy/m03_signals.py:67`**
   _attention_deep_sig() (and _ema_signal()) hardcode threshold judgments directly in the signals module, violating the README's stated 'signals only computes numbers, no threshold judgment' boundary.
   > `_attention_deep_sig` buckets `deep_mentions` into STRONG/MIXED/NONE using hardcoded cutoffs (`n >= 3`, `n >= 1`) baked into m03_signals.py rather than read from `cfg['scoring']['tiers']` the way every other tier's thresholds are (m04_scoring.py reads `thr.get(...)` for all other tiers). Likewise `_ema_signal` buckets EMA alignment into BULLISH/BEARISH/NEUTRAL using a hardcoded `aligned >= 2` cutoff. Per `demo_strategy/README.md` ('signals 与 scoring 严格分离 — signals 只算数字，scoring 只按阈值给分。调参不会碰指标计算') and `square_buzz_screener/README.md` ('m03_signals.py ... 无阈值判定'), all threshold tuning should live in m04/config so it can be retuned without touching indicator code. A teammate wanting to change the 'STRONG sentiment' cutoff from 3 to 5, or the EMA-alignment bar from 2/3 to 3/3, would have to edit m03_signals.py instead of config.json, defeating the documented separation of concerns this demo is meant to teach.

46. **`demo_strategy/square_buzz_screener/strategy/m04_scoring.py:36`**
   _verdict_5tier() hand-rolls the STRONG_CANDIDATE/CANDIDATE/WATCHLIST/SKIP threshold ladder instead of reusing the already-existing verdict_classify/verdict_with_gate atoms.
   > The manual cascade `if total >= strong: STRONG_CANDIDATE ... elif total >= cand: CANDIDATE ...` duplicates `cyqnt_trd/blocks/verdicts.py:335 verdict_classify(score, strong_min, candidate_min, watchlist_min, skip_min)` and `:354 verdict_with_gate(score, gates, cap_verdict, ...)`, both already exposed as `atomic_strategy_lib.scoring.gates.verdict_classify` / `verdict_with_gate` in the exact same package tree m04_scoring.py already imports from (`atomic_strategy_lib.scoring.combinators`). m04_scoring.py never imports `atomic_strategy_lib.scoring.gates` at all. If the canonical ladder semantics ever change (e.g. boundary inclusivity, an extra tier, or gate-capping behavior), square_buzz_screener's hand-rolled version silently drifts from every other strategy using verdict_classify/verdict_with_gate, producing different verdicts for the same score.

47. **`demo_strategy/square_buzz_screener/strategy/m06_execution.py:72`**
   _place_one() re-derives the percent-based stop price with inline arithmetic instead of calling the existing fixed_pct_stop atom, duplicating tested stop-loss math.
   > `stop_px = round_to_tick(entry * (1 - decision["stop_pct"]/100) if decision["direction"]=="LONG" else entry * (1 + decision["stop_pct"]/100), filt)` duplicates `cyqnt_trd/blocks/stop_loss.py:39 fixed_pct_stop(entry_price, direction, stop_pct=5.0)`, which computes the identical `entry*(1±stop_pct/100)` and is already reachable one import away as `atomic_strategy_lib.risk.stop_loss.fixed_pct_stop` (m06 already imports a sibling module, `atomic_strategy_lib.risk.limits`, from the same `risk` package). Because the direction check here is a strict string equality against `"LONG"` (case-sensitive) rather than `fixed_pct_stop`'s `direction.upper()=="LONG"`, and because it isn't backed by the atom's rounding/edge-case handling, this inline copy can silently diverge from the canonical stop-price computation used elsewhere in the codebase if either implementation is tweaked.


---

## 被推翻的發現(7 個,附紀錄以示嚴謹)

對抗驗證階段有 7 個原始發現被判定不成立(通常是「敘述的失敗機制在真實程式碼裡不會照那個路徑發生」——例如 square_buzz_screener 的 `_place_one` 缺少 entry/stop 成功與否檢查這個說法沒錯,但因為前面 `qty=` 參數名稱錯誤會先讓整個呼叫直接 crash,所以「靜默記錄成功」這個具體情境根本走不到)。這些不在上面清單裡,細節可從 workflow 原始 log 調閱。
